#include <stdio.h>
#include <stdlib.h>

#include "test.h"
#include "tools.h"
#include "matrix.h"

int main(int argc, char **argv) {
    test_function_t tests[] = {
    };

    test_run(tests, ARRAY_LEN(tests));
}
